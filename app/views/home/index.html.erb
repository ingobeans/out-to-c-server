<script>
    // load user data for the JS side
    voyage = parseInt('<%= @voyage && @voyage.id %>') || null;
    _travelDistance = (parseFloat('<%= @voyage && @voyage.hours %>') || 0.0);

    _activeCameraStateIndex = voyage == null ? 0 : 2;
</script>

<style>
    canvas {
        filter: unset !important;
    }
</style>

<div id="background"></div>
<div id="varyingContents">
    <img id="pfp" src="<%= session[:user_id]["pfp"] %>">

    <div id="no-existing-voyage" <% if @voyage != nil %> style="visibility: hidden;"<% end %>>
        <button onclick="newVoyage();" id="new-voyage">New Voyage</button>
        <div id="new-voyage-div" class="page">
            <h1>New Voyage</h1>
            <%= form_with(url: voyage_new_path, id: "new-voyage-form") do |form| %>
                <label for=name>Name: </label><br>
                <input autocomplete="off" name="name" type="text">
                <br>
                <label for=name>Description (optional): </label><br>
                <input autocomplete="off" name="name" type="text">

                <br>
                <label for=hackatime>Select Hackatime project: </label><br>

                <select autocomplete="off" name="hackatime" id="hackatime">
                    <option value="" disabled selected>Select a Hackatime project</option>
                    <option value="blurb">blurb</option>
                    <option value="cool project">cool project</option>
                    <option value="test">test</option>
                    <option value="lorem ipsum">lorem ipsum</option>
                </select>
                <br>
                <button>CREATE VOYAGE</button>
            <% end %>
        </div>
    </div>
    <div id="active-voyage" <% if @voyage == nil %> style="visibility: hidden;"<% end %>>
        <div class="page">
            <h2>Dev panel</h2>
            <%= form_with(url: voyage_add_hour_path) do |form| %>
                <button onclick="setTravelDistance(travelDistance+1)">add 1 Hour</button>
            <% end %>
            <%= form_with(url: voyage_delete_path) do |form| %>
                <button>remove voyage</button>
            <% end %>
        </div>
    </div>
</div>
